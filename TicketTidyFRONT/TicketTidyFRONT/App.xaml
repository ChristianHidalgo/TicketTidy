<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TicketTidyFRONT"
             x:Class="TicketTidyFRONT.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <ResourceDictionary>
                <Style TargetType="NavigationPage">
                    <Setter Property="BarBackgroundColor" Value="{StaticResource Gray100}" />
                    <Setter Property="BarTextColor" Value="{StaticResource PrimaryTextColor}" />
                </Style>
            </ResourceDictionary>

            <Color x:Key="MenuBackground">#FFFFFF</Color> <Color x:Key="MenuItemIconBackground">#007BFF</Color> <Color x:Key="MenuItemIconForeground">#FFFFFF</Color> <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource PageBackgroundColor}" />
            </Style>

            <Style x:Key="ProfileFrameStyle" TargetType="Frame">
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="HasShadow" Value="True" />
                <Setter Property="BackgroundColor" Value="{StaticResource White}" />
                <Setter Property="Margin" Value="0,0,0,15" />
            </Style>

            <Style x:Key="ProfileImageBorder" TargetType="Border">
                <Setter Property="StrokeShape" Value="RoundRectangle 50,50,50,50" />
                <Setter Property="StrokeThickness" Value="0" />
                <Setter Property="WidthRequest" Value="80" />
                <Setter Property="HeightRequest" Value="80" />
                <Setter Property="HorizontalOptions" Value="Center" />
            </Style>

            <Style x:Key="ProfileNameLabel" TargetType="Label">
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Margin" Value="0,10,0,0" />
            </Style>

            <Style x:Key="ProfileEmailLabel" TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{StaticResource SecondaryTextColor}" />
                <Setter Property="HorizontalOptions" Value="Center" />
            </Style>

            <Style x:Key="ProfileIdLabel" TargetType="Label">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="TextColor" Value="{StaticResource SecondaryTextColor}" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Margin" Value="0,5,0,0" />
            </Style>

            <Style x:Key="SeparatorStyle" TargetType="BoxView">
                <Setter Property="HeightRequest" Value="1" />
                <Setter Property="BackgroundColor" Value="{StaticResource Gray300}" />
                <Setter Property="Opacity" Value="0.3" />
                <Setter Property="Margin" Value="0,15" />
            </Style>

            <Style x:Key="MenuItemIconFrame" TargetType="Frame">
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="BackgroundColor" Value="{StaticResource MenuItemIconBackground}" /> <Setter Property="WidthRequest" Value="40" />
                <Setter Property="HeightRequest" Value="40" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="HasShadow" Value="False" />
            </Style>

            <Style x:Key="MenuItemLabel" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="Margin" Value="15,10,0,10" /> </Style>

            <Style x:Key="MenuBackgroundColorStyle" TargetType="ListView">
                <Setter Property="BackgroundColor" Value="{StaticResource MenuBackground}" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>